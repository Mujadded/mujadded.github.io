# mjalif.com — everything here is meant to be indexed.
#
# The rules below apply to every crawler. They previously sat underneath a
# chain of per-bot "User-agent" groups, which meant the Disallow lines and the
# crawl delay applied to YandexBot alone and to nobody else; the per-bot groups
# themselves only repeated "Allow: /", so they did nothing.

User-agent: *
Allow: /
Disallow: /admin/
Disallow: /private/

Sitemap: https://mjalif.com/sitemap.xml
